# Docker Composer Wordpress demo

# Run Demo with Docker Compose

## Build and run Docker Compose

```bash
docker compose up -d
```

## Rebuild images

```bash
docker compose up --build -d
```


## Delete all

```bash
docker compose down --volumes
```

## Wordpress config site

http://localhost/wp-login.php
